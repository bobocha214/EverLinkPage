# 2026-08-09 工作记录

## Web 端图标微调 + 修复抖动
- `docs/.vitepress/theme/style.css`：
  - 图标基线再右移：`.VPHero .image` 的 `translateX` 由 `120px` 增至 `145px`（translateY48 不变），`@keyframes el-float-icon` 同步改为 `translateX(145px)`。
  - 修复图标"抖动/摇晃"：`el-float-icon` 关键帧原本带 `rotate(1.2deg)` 来回摆动，视觉上像在晃；已去掉 rotate，仅保留平滑的上下浮动（48px↔36px，6s ease-in-out alternate）。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过。
  - dev server 启动于 http://localhost:5178/（5176/5177 被上一轮残留实例占用，自动顺延）。
- 说明：本机仍无 Chromium，无法截图；图标位置/平滑动画请在预览面板确认。

## Web 端图标继续右移 + 修复手机端"多余方框"
- 继续右移：`.VPHero .image` 的 `translateX` 由 `145px` 增至 `165px`（translateY48 不变），`@keyframes el-float-icon` 同步改为 `translateX(165px)`。
- 修复手机端图标外多余圆角方框：该"方框"是 `.VPHero .image img` 的 `box-shadow` 在窄屏下显形造成的；在 `@media (max-width:960px)` 中新增 `.VPHero .image img { box-shadow: none; }`，手机端图标不再带那圈青色投影。
- 清理了残留的旧 dev server 进程（5176-5178 被占用），新 server 已固定在 http://localhost:5176/。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过。

## 修正目标受众定位
- 用户纠正：`EverLink` 受众不是"Android 工程师"，而是**工控 / 设备调试工程师**（偏工业控制场景）。
- `docs/index.md` 的 hero tagline 由"专为 Android 工程师打造"改为"专为工控与设备调试工程师打造"（协议清单保留不变）。
- 核对 `docs/guide/introduction.md`：本就写的是"面向工业现场与物联网场景的 Android 设备协议调试工具 / 工业自动化·设备维护工程师"，表述正确，无需改（其中的"Android"指运行平台，非受众）。
- 注：产品本身是 Android App，"Android"作为平台词仍保留在 introduction 等说明中，属合理。

## 首页标语去指向性 + 修复手机端菜单透明
- `docs/index.md`：hero tagline 再次调整，去掉"专为工控与设备调试工程师打造"的指向性表述，改为纯软件介绍："一站式 Modbus TCP / MQTT / WebSocket / HTTP / OPC UA 调试与局域网工具箱，工业现场的移动协议调试工作站。"
- `docs/.vitepress/theme/style.css`：
  - 修复手机端菜单/抽屉透明：之前 `.VPNavBar, .VPSidebar, .VPContent...` 全设为 `background: transparent !important;`，导致移动端 `.VPNavScreen` 菜单背景透明、文字与正文重叠。已将 `.VPNavBar` 和 `.VPSidebar` 移出全局透明规则；并单独为 `.VPNavScreen`、`.VPSidebar` 设置 `background: var(--vp-c-bg) !important;`，`.VPNavBar` 设为半透明毛玻璃背景，确保移动端菜单可读。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过。
- dev server 已重启并固定在 http://localhost:5176/。

## 增强背景彩虹渐变光影 + 安装页补齐多平台
- 背景光影增强（`docs/.vitepress/theme/style.css`）：
  - `--el-glow-opacity` 由 `0.60` 提至 `0.72`（浅色模式光晕更明显）。
  - 新增流动彩虹渐变层 `body::after`：固定全屏、`z-index:-2`、`conic-gradient`（青绿主调 + 蓝紫/黄绿相邻色）、`blur(95px)`、`opacity:0.55`、`el-rainbow-spin 30s linear infinite` 缓慢旋转；并加入 `prefers-reduced-motion` 关闭列表。
  - Hero 后方光斑 opacity `0.5`→`0.6`。
- 安装页多平台文档（`docs/guide/install.md`）：
  - 参考 Flutter 项目 `D:\android\projects\everlink`（包名 `com.everlink.app`、版本 1.0.0+1、iOS Runner 完整且 AppIcon 已由 `flutter_launcher_icons` `ios:true` 生成）。
  - 顶部加一句引导；新增「其他平台（自行编译）」节：说明官方仅分发 Android APK，其他平台（iOS/Windows/macOS/Linux/Web）需开发者自行 `flutter build`，并给出各平台命令与 iOS 签名/Bundle ID 提示。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过；dev server HMR 已生效。

## 项目事实备忘（EverLink Flutter App）
- 真实发布渠道：GitHub Releases `everlink-app/EverLink`、Gitee Releases `everlink-app/EverLink`。
- 应用包名 `com.everlink.app`；当前版本 1.0.0+1；编译 compileSdk=36、minSdk 由 Flutter 默认（launcher icons 配置 min_sdk_android:21）。
- 依赖含 modbus_client / mqtt_client / mcp_io_opcua（OPC UA）/ webview_flutter / qr_flutter 等，对应"多协议调试 + 局域网快传 + 扫码"功能。
- iOS 目录是 Flutter 默认 Runner 模板，图标已生成，但未配置签名/发布（需 Xcode 配 Team + Bundle ID）。

## 图标改为青绿渐变背景（解决"标题有渐变、图标没有"观感）
- 用户疑问：为何标题区有渐变背景、图标却是实色。根因：标题文字透明，后方的装饰光晕（`.VPHome .VPHero::before` 超大径向渐变 + 全局 `body::after` 流动彩虹 conic 渐变）透出来，显得"标题带渐变"；而 `ic_launcher.svg` 是不透明实色方块 `#00897B`，把身后的辉光盖住了，所以图标看起来是平板实色。
- 修复：`docs/public/ic_launcher.svg` 的 `<rect>` 由 `fill="#00897B"` 改为 `fill="url(#el-bg)"`，新增 `<linearGradient id="el-bg">`（teal #0d9488 → #14b8a6 → cyan #06b6d4，对角），使图标自身带渐变、与站点发光色调一致。
- 注：此前"彩虹动画"实际是背景流动光影（作用于整个页面背景），标题文字早已改为静态纯色；用户这次指的"标题渐变背景"其实是标题后方透出的光晕，非标题文字本身的渐变。

## 撤销图标填充渐变 + 去掉图标底部渐变辉光（用户澄清）
- 用户澄清：上轮把图标 `ic_launcher.svg` 填充改成青绿渐变并非其本意；真正指的是「图标底部的渐变」——即图标 `img` 的 `box-shadow` 中向下偏移 18px+模糊 52px 的青绿辉光（看起来像图标底部拖一道渐变），对比标题整片居中光斑显得不一致。
- 处理：
  - 撤销 `ic_launcher.svg` 的 `<rect>` 渐变填充，恢复实色 `#00897B`（与真实 App 启动图标一致）。
  - `docs/.vitepress/theme/style.css`：`.VPHero .image img` 的 `box-shadow` 改为 `none`；`.VPHero .image::before`（图标背后偏底部的模糊光斑）设为 `display:none`。图标现为干净实色方块，仅保留 `.image::after` 极淡连接轨道环。
  - 用户选择「去掉只留干净图标」。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过。

## 图标背景改为明显青绿渐变（用户最新反转要求）
- 用户最终要求：图标背景要像截图中那样「明显的渐变」（与上轮「去掉改为干净实色」相反）。
- `docs/public/ic_launcher.svg`：`<rect>` 由实色 `#00897B` 改回渐变填充 `url(#el-bg)`（teal #0d9488 → #14b8a6 → cyan #22d3ee，对角 14%→86%），并新增斜向高光 `url(#el-shine)`（白色 0.42→0.08→0 透明度），渐变明显且带玻璃质感。白色连接符号几何保持不变（1:1 还原 ic_launcher.png）。
- 注：用户截图（@image#1:Clipboard_Screenshot.png）不在磁盘、无法查看像素；按整体「青绿色背景色 + UnoCSS 渐变质感」基调采用青绿系渐变。若用户实际想要的是彩色彩虹（如 UnoCSS logo）渐变，可再调整。

## 彩虹动画定位修正 + 去除两张功能卡片（用户最终澄清）
- 用户澄清：最初要的「彩虹动画」是**图标背景色**（参考 UnoCSS），不是全站/标题；「图标颜色用纯色」指白色连接符号纯色。
- `docs/.vitepress/theme/style.css`：
  - 删除全站彩色流动彩虹层 `body::after`（conic-gradient + el-rainbow-spin 旋转），全站回归纯青绿光斑氛围（html/body 伪元素），契合「青绿色的背景色」。
  - `.VPHero .image` 容器改为**彩虹流动渐变背景**（linear-gradient 115° 多彩 + background-size:200% + `el-rainbow 5s linear infinite` 平移），圆角 25%，白色符号浮其上；同时保留 `el-float-icon` 上下浮动（合并为两个 animation）。新增 `@keyframes el-rainbow`。从 reduced-motion 移除已删 body::after 引用。
- `docs/index.md`：
  - hero image 由 `/ic_launcher.svg` 换回 `/icon_foreground.svg`（透明背景、白色纯色连接符号），alt 改「EverLink 连接符号」。
  - 删 features 中「网络调试」「剪贴板管理」；现剩 3 张：多协议调试、设备集中管理、局域网快传。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过。

## 修复 icon_foreground.svg 图标走形（本轮）
- 现象：用户反馈 hero 右侧图标「弄坏了」。排查发现 `docs/public/icon_foreground.svg` 被写成 108×108 视图、双大圆 R=25 @32,32/76,76、中心节点 R=8、连杆宽14——比例严重放大（与参考图 `icon_foreground.png` 像素重叠率仅 18.82%，符号面积约大 5.3 倍），且大圆贴近视图边缘，被外层 25% 圆角裁切掉边，视觉走形。
- 修复：恢复为正确几何（192×192 视图，与 `ic_launcher.svg` 同比例）：双大圆 R=17 @67,67/124,124、中心节点 R=8.5 @95.5,95.5、连杆 stroke-width=6、透明背景、纯白符号。恢复后与参考图重叠率 91.7%。
- 校验：Pillow 以 192→1024 比例栅格化 SVG 几何，与 `icon_foreground.png` / `ic_launcher_foreground.png` 逐像素比对确认。
- dev server 现运行于 http://localhost:5176/，SVG 资源返回 200。

## 参考 NexIoT 文档站重构首页风格 + 图标按 ic_launcher.png 设计
- 用户要求：布局与背景颜色参考 https://docs.nexiot.cc/；图标按 `ic_launcher.png` 设计。
- 调整 `docs/.vitepress/theme/style.css`：
  - 全站背景回归 VitePress 默认干净白底 / 深色背景，删除此前的 html/body 青绿 aurora 光晕伪元素。
  - Hero 标题 `.VPHero .name` 改为彩虹渐变文字（`background-clip:text`），并添加 `el-rainbow` 流动动画。
  - Hero 图标后方由 `.VPHero .image::before` 承载模糊彩虹光晕（参考 NexIoT `--vp-home-hero-image-background-image` + `blur(70px)`），同样带动画。
  - 删除图标的右移/上下浮动/轨道环，使用 VitePress 默认左右 Hero 布局。
  - 品牌色改为 `#00897B`（与 ic_launcher.png 一致），按钮与卡片描边使用青绿。
  - 特性卡片改为干净白底（`--vp-c-bg-soft`）+ 淡青绿描边；正文文档卡片简化。
- 调整 `docs/public/ic_launcher.svg`：背景由青绿对角渐变恢复为**实色 #00897B**，与 `ic_launcher.png` 完全一致。
- 调整 `docs/index.md`：hero image 由 `/icon_foreground.svg` 换回 `/ic_launcher.svg`，alt 改为「EverLink 应用图标」。
- 验证：`NODE_OPTIONS= npm run docs:build` 通过；dev server 重启并绑定 127.0.0.1:5176，首页与 SVG 均返回 200。
